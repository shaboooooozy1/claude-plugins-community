name: Validate Plugins

on:
  pull_request:
    paths:
      - '.claude-plugin/**'
      - '.github/actions/**'
  push:
    branches: [main]
    paths:
      - '.claude-plugin/**'
      - '.github/actions/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Static invariant tests
        run: bash .github/actions/validate-plugins/test-invariants.sh

      - name: Static common.sh predicate tests
        run: bash .github/actions/validate-plugins/test-common.sh

      - name: Static detect-changes tests
        run: bash .github/actions/validate-plugins/test-detect-changes.sh

      - name: Static aux-files and report tests
        run: bash .github/actions/validate-plugins/test-aux-and-report.sh

      - name: Static cli-external tests
        run: bash .github/actions/validate-plugins/test-cli-external.sh

      - name: Static cli-local tests
        run: bash .github/actions/validate-plugins/test-cli-local.sh

      # Dogfood: this repo uses its own local copy of the action.
      # External consumers MUST pin a SHA — see the action README.
      - uses: ./.github/actions/validate-plugins
        with:
          marketplace-path: .claude-plugin/marketplace.json
          skip-local-folders: "true"
